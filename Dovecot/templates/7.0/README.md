# Dovecot Template for Zabbix 7.0

Status: current validated template.

Import `Template_Dovecot_7.0.yaml` into Zabbix 7.0 and use the root `dovecot_stats.sh` and `userparameter_dovecot.conf` files on monitored Dovecot hosts.

