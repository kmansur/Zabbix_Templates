name: Test UniFi update runner

on:
  push:
    branches:
      - feature/unifi-network-10-6
    paths:
      - .github/workflows/unifi-network-10-6-update.yml

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo runner-ok
