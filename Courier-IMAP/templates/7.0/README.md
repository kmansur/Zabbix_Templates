# Courier-IMAP Template for Zabbix 7.0

Status: current validated template.

Import `Template_Courier_IMAP_7.0.yaml` into Zabbix 7.0 and use the root `scripts/` and `agent/userparameter_courier.conf` files on monitored Courier-IMAP hosts.
